dependencies {
	compile project(":modules:service-module:service-module-api")

	compileOnly group: "com.liferay.portal", name: "release.portal.api"
}

buildService {
	apiDir = "../service-module-api/src/main/java"
}

group = "com.aixtor.intranet"